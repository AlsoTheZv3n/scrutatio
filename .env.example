# Copy to .env and fill in. Never commit the real .env.
#
# Databricks workspace URL — the part before "/?o=" in your browser address bar,
# e.g. https://dbc-xxxxxxxx-xxxx.cloud.databricks.com
DATABRICKS_HOST=

# Personal Access Token: Settings -> Developer -> Access tokens -> Generate new token
DATABRICKS_TOKEN=
